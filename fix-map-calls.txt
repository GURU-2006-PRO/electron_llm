Find and replace in renderer_simple.js:

FIND:
                console.log('[MAP] Rendering India map');
                if (typeof renderIndiaMap === 'function') {
                    renderIndiaMap(response.data.map_data);
                } else {
                    console.warn('[MAP] renderIndiaMap function not available');
                }

REPLACE WITH:
                console.log('[MAP] Map data received, showing prompt');
                showMapGenerationPrompt(response.data.map_data);

Do this replacement 2 times (both occurrences in the file)
